<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="auto.ausiot.autosensor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InitViewer"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:screenOrientation="portrait"/>
        1
        <activity android:name=".EventViewer"  android:screenOrientation="portrait"/>
        <activity android:name=".MonitorActivity" android:screenOrientation="portrait"/>
        <activity android:name=".AddUser" android:screenOrientation="portrait"/>
        <activity android:name=".AppSettings" android:screenOrientation="portrait"/>
        <activity android:name=".Disclaimer" android:screenOrientation="portrait"/>

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true" />

    </application>

</manifest>