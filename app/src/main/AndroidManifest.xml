<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="auto.ausiot.autosensor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InitViewer"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".EventViewer"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MonitorActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".UserInfo"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AppSettings"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Disclaimer"
            android:screenOrientation="portrait" />

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true" />
        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true" />

        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"></activity>
        <activity
            android:name=".RegisterUser"></activity>
        <activity
            android:name=".ManageSchedulesActivity"></activity>

        <activity
            android:name=".RepeatScheduleActivity"></activity>

        <activity
            android:name=".SingleScheduleActivity"></activity>

        <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyDiW-WR6E5cmlBLdjyuQK0etJP4Iwf_5i0"/>
    </application>

</manifest>